# migrations/

One-off SQL scripts that were run manually against the database during development.
These are kept here for reference only — they do not run automatically.

## add_multilingual.sql

Adds `name_he` and `description_he` columns to the `product` table, and `name_he` to the `categories` table.
Already applied to the local development database.

